local BASH = BASH;
BASH.Items = BASH.Items or {};
BASH.Items.Name = "Items";
BASH.Items.Bases = BASH.Items.Bases or {};
BASH.Items.Entries = BASH.Items.Entries or {};

function BASH.Items:Init()

end

function BASH.Items:NewBase(base)

end

function BASH.Items:NewItem(item)

end

BASH:RegisterLib(BASH.Items);
