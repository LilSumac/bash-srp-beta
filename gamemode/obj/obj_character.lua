local BASH = BASH;
BASH.Character = {};
BASH.Character.Loaded = {};
local CHAR_meta = {};

function BASH.Character:New(charData)
    if !charData or !charData.CharID then
        local args = concatArgs()
    end
end
